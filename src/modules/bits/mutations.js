export default {
    SET_BIT_PRODUCTS(state,products){
        state.bitProducts = products;
    },
    SET_HAS_ONGOING_BIT_TRANSACTION(state,hasOngoingBitTransaction){
        state.hasOngoingBitTransaction = hasOngoingBitTransaction;
    }
}